import "./globals.css";

export const metadata = {
  title: "Nhãn vở & Sticker cá nhân hóa",
  description: "Chọn mẫu, nhập thông tin và đặt in online."
};

export default function RootLayout({ children }) {
  return <html lang="vi"><body>{children}</body></html>;
}
